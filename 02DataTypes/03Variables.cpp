#include <iostream>

int main() {
    // Variable declaration.
    int salary = 40000;
    std::string firstName = "Vamtiger";
    double rate = 0.2 * 0.3;
    int age = 20;

    std::cout << firstName << " is " << age << " years old, and earns " 
        << salary << " at a rate of " << rate << ".\n";
}